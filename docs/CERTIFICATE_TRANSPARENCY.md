# Certificate Transparency review

## IMPLEMENTED

The source registry contains a CT candidate with `DISABLED_TERMS`. This documents the intended role as DOMAIN-to-certificate-name discovery/enrichment and explicitly prevents execution.

## NOT IMPLEMENTED

No CT endpoint is enabled because an automation interface and applicable terms were not sufficiently established. Shared certificates must never be interpreted as ownership.
